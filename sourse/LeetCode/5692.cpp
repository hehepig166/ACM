#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    vector<double> getCollisionTimes(vector<vector<int>>& cars) {
		int n=cars.size();
		
		for (int i=n-2; i>=0; i--) {
			if ()
		}
    }
};

int main()
{
	vector<vector<int>> A={};
	Solution S;
	
}
